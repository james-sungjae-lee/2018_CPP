#include <iostream>
using namespace std;

// n 개의 원소를 가지는 배열의 경우, 첫 번째 원소의 번호는 0

// n개의 원소 .. 마지막 원소의 번호는 n-1

// 범위를 벗어나는 인덱스 사용시? 대입은 정상적으로 되고, 불러오기도 되지만, 문제가 있다.

// 배열의 초기화에서 초기값이 개수가 배열 원소의 개수보다 적은 경우에는 나머지 값이 0으로 초기화
// 많은 경우에는? 컴파일 에러

// 배열의 크기를 주지 않고 초기값의 개수로 배열의 크기를 결정할 수 있는가?

// 함수의 매개 변수로 전달된 배열을 변경하면 원본 배열이 변경되는가?
// 변경되지 않는다.

// 배열을 전달받은 함수가 배열을 변경하지 못하게 하려면 const를 앞에 써준다.









//
