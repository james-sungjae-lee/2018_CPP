// 만약 클래스 이름이 MyClass 라면, 생성자의 이름은?
// MyClass();

// 생성자의 반환형은?
// 반환값이 없지만, void를 붙이지도 않는다!

// 생성자는 중복 정의가 가능하다.
// 매개 변수에 따라 달라진다.

// 클래스 안에 생성자를 하나도 정의하지 않으면,
// 알아서 생성자를 정의한다. - 컴파일러가!

// 만약 클래스 이름이 ~ ~
// 소멸자 반환형 x 소멸자 중복 정의 x

// 초기화를 반드시 해야 하는 타입은?
// 상수 멤버 / 레퍼런스 멤버

// limit 을 초기화 리스트로 초기화하라 : limits(100);
